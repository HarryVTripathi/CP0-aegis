 - Arrays.fill(prototypeArray, fillValue)

 - Arrays.sort(originalArray, fromIndex, endIndex)

 - Arrays.sort(T[] a, Comparator< super T> c)

 - Arrays.binarySearch(elementToBeSearched)

 - array.length
