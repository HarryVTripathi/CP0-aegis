from collections import defaultdict

def maxLenAP(arr, n):
  memo = defaultdict(lambda: defaultdict(lambda: 0))

  for i in range(n):
    a = arr[i]
    for j in range(i+1, n):

  return 0


maxLenAP([], 0)
